services:
  labspace:
    environment:
      # LabSpace configuration
      - LABSPACE_TITLE=Testcontainers Cloud Lab for TSEs
      - LABSPACE_DESCRIPTION=Master TCC through hands-on exercises and real TSE scenarios
    volumes:
      # Mount lab content
      - ./labspace-exercises:/workspace/labspace-exercises:ro
      - ./labspace.yaml:/workspace/labspace.yaml:ro
      - ./README.md:/workspace/README.md:ro